# Convolution example showing parameterized weights and stride attribute
# Ops: Conv

@fuse 1.2
@opset onnx 18
@domain examples.golden
@meta author = "onnx.cloud"

node conv(x: f32[1, 28, 28], w: f32[64, 3, 7, 7]) -> f32[64, 22, 22] {
  y = Conv(x, w, stride@=2)
  y
}
