# List fingerprints used across motifs
PREFIX motif: <https://ns.onnx.cloud/motif#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

SELECT DISTINCT ?f ?label WHERE {
  ?motif motif:hasFingerprint ?f .
  OPTIONAL { ?f skos:prefLabel ?label }
}
ORDER BY ?label
